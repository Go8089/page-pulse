services:
  backend:
    build: ./backend
    ports:
      - "8085:8085"
    env_file:
      - ./backend/.env.example

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend